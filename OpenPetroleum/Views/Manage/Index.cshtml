@model OpenPetroleum.Models.IndexViewModel
@using Microsoft.AspNet.Identity
@{
    ViewBag.Title = "Open Petroleum Admin Dashboard";
}
<p class="text-success">@ViewBag.StatusMessage</p>
<div class="container">
    <h2>@ViewBag.Title</h2>
    <h4>Welcome @User.Identity.GetUserName()</h4>
    <hr />
    <div class="col-lg-3"> 
        <h3>Account</h3>
        <p>@Html.ActionLink("Change my password", "ChangePassword")</p>
    </div>
    <div class="col-lg-3">
        <h3>Application</h3>
        <p>@Html.ActionLink("Add / Remove User", "ChangePassword")</p>
    </div>
    <div class="col-lg-3">
        <h3>Data</h3>
        <p>@Html.ActionLink("View Data", "ChangePassword")</p>
        <p>@Html.ActionLink("Modify Data", "ChangePassword")</p>
    </div>
    </div>
